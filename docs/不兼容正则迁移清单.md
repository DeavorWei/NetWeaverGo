# Go RE2 正则兼容性扫描与迁移清单

> 扫描目标: `internal/parser/templates` | 扫描文件数: 3 | 提取模式数: 38 | 目标资产问题数: 0

## 一、语法特征兼容性对照表

| 语法特征 | RE2 支持状态 | 推荐迁移与适配策略 |
|---|---|---|
| 正向后行断言 `(?<=...)` | ❌ 不支持 | 改写为普通捕获组并在代码中按 GroupIndex 提取，或用前后分块定界 |
| 负向后行断言 `(?<!...)` | ❌ 不支持 | 改写为字符集排除 `[^...]` 或在匹配后通过 Go 代码过滤 |
| 正向先行断言 `(?=...)` | ❌ 不支持 | 改写为普通捕获组合并匹配 |
| 负向先行断言 `(?!...)` | ❌ 不支持 | 改写为字符类排除或后置状态机过滤 |
| 反向引用 `\1`, `\2` | ❌ 不支持 | 拆分为两次匹配并在业务层比对相等性 |

## 二、扫描目标资产检测结果

✅ **目标范围内的所有正则表达式均 100% 兼容 Go RE2 引擎，零不兼容语法阻碍。**

## 三、常见不兼容语法基线对照与改写示范（参考基准）

以下为典型 Python/PCRE 正则在迁移至 Go RE2 时的标准改写示范（不计入目标资产问题数）：

| 样例场景 | 原始 PCRE 正则 | 不兼容特征 | Go RE2 标准改写指引 |
|---|---|---|---|
| 正向后行断言样例 | `(?<=Slot_)\d+` | **Positive Lookbehind (?<=...)** | Go RE2 不支持。改写为普通捕获组 `Slot_(\d+)`并在代码中提取第 1 组，或以 `Slot_` 为分块边界。 |
| 正向先行断言样例 | `interface (?=GigabitEthernet)` | **Positive Lookahead (?=...)** | Go RE2 不支持。改写为 `interface (GigabitEthernet)` 普通捕获组。 |
| 反向引用样例 | `<(\w+)>.*?</\1>` | **Backreference (\1, \2...)** | Go RE2 不支持。需在匹配后于业务代码中比对首尾标签是否一致。 |

