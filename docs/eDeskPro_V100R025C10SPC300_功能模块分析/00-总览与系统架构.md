# 华为 eDesk Pro V100R025C10SPC300 — 功能模块分析总览

> 分析对象：`E:\BaiduDownload\eDeskPro_V100R025C10SPC300-windows-x64`（**完整交付包**，24086 文件）
> 版本：eDesk Pro V100R025C10SPC300（windows-x64）
> 目的：按目录 / 功能模块拆解官方巡检工具，为 **NetWeaverGo 能力迁移**提供模块级清单。
> 关联：`docs/华为eDeskPro脚本体系分析.md` 仅覆盖 `product/Script` 子集；本系列补全其余全部目录。

---

## 1. 与旧分析的边界

旧文档只看了 `product/`（4148 py + 169 xml + 30 csv）。本完整包新增了大量**之前缺失的架构层**：

| 层 | 旧分析是否覆盖 | 本包位置 |
|---|---|---|
| 采集脚本库（Jython） | ✅ 部分 | `product/Script/` |
| 采集项注册表 | ✅ 部分 | `product/CollectItem/` |
| **配置审计规则引擎**（8312 py） | ❌ 缺失 | `services/EMTMessageAnalyseClientService/` |
| **Java 巡检解析后端** | ❌ 缺失 | `services/IPDeviceCheckService/` |
| **采集编排 / 协议层**（NMOT） | ❌ 缺失 | `services/NMOT*` + `NetCareInsideService` |
| **数据库模型** | ❌ 缺失 | `dbScript/` |
| **配置体系 / 应用模块 / 启动** | ❌ 缺失 | `config/`、`apps/`、`bin/` |

---

## 2. 包目录全景

```
eDeskPro_V100R025C10SPC300-windows-x64/
├── product/         4513 文件  采集脚本库（旧分析主体）         → 见 01
│   ├── CollectItem/   注册表（XML，厂商×产品→脚本）
│   ├── Script/        4148 py（common/ceas/inspector/combination/getversion）
│   ├── Template/      巡检模板
│   ├── Resource/      多语言文案 CSV
│   └── config/       风险命令、一屏展示
├── services/        18458 文件  Java 后端 + Python 引擎
│   ├── EMTMessageAnalyseClientService/  8312 py 配置审计规则 + 采集器  → 见 02
│   ├── IPDeviceCheckService/  347 class 回显解析 / 弱光光纤 / 链路     → 见 03
│   ├── NMOTBusinessService/      编排业务层（设备连接参数、代理、任务）
│   ├── NMOTCollectAppService/    采集应用层（CollectService、Agent 推送）
│   ├── NMOTPlatformService/      平台/网关/安全/文件传输
│   ├── NetCareInsideService/     后端↔客户端 REST 控制面（端口 51943，非协议层）
│   ├── NmotLicenseService/        License 鉴权
│   ├── IPDeviceCheckWebsite/  NMOTPlatformWebsite/  NMOTCollectAppWebsite/  前端
│   └── uupgrade/        3821 文件  升级包（json/xml/sql）
├── config/          315 文件   运行配置                              → 见 05
│   ├── devicemapping/   各厂商 productItem.xml（款型→类型）
│   ├── versionmapping/  inspect/ unetbuilder/ 版本映射
│   ├── validate/        55 个 XML 入参校验规则
│   ├── script/          接口名转换/过滤
│   ├── faultConfig/ multihop/ filetransfer/ log4j/ ...
│   └── *.properties     cmd/ inspect/ connect/ taskConfig/ 安全白名单...
├── apps/            应用模块定义（5 个 APP）                        → 见 07
│   ├── Easy-Health/ Easy-Diagnosis/ Easy-RFC/ FiberOptics-Check/ Platform-App/
├── dbScript/        57 sql     数据库 schema                       → 见 06
│   ├── global/ network/ unicnetwork/ unicollect/ log/
├── bin/  launcher/  start.bat/stop.bat  启动与运维                  → 见 05
├── lib/ 平台依赖（258 jar）   jre/ Java 运行时   platform/ 二进制
└── get_log.bat
```

---

## 3. 技术栈与运行模型

| 维度 | 实现 |
|---|---|
| 前端 | Electron + Vue（`nmotplatformwebsite` / `nmotcollectappwebsite`） |
| 后端 | Java（Spring Boot 3.5 / Tomcat 10.1）多 WAR 微服务，REST 前缀 `/rest/nmot/*` |
| 脚本执行 | **Python(Jython)** 经 `nmotbusinesscbb` 引擎执行；SNMP 回调端口 **UDP 38887**（CBB 内部，非独立 Jython 服务，详见 `04` §8） |
| 设备协议 | **桌面客户端二进制**（SSH / Telnet / Netconf / SNMP / Serial，不在本包；`netcareinside-driver` jar 实为后端↔客户端 REST 控制面，端口 51943） |
| 解析引擎 | 声明式 XML 正则规则（`parsecfg/xmlconfig/<vendor>/*.xml`）+ 字段算子 ETL |
| 存储 | 关系型（H2/MySQL，由 `dbScript/` 建模）+ 文件（NIC 压缩包、CSV 报表） |

---

## 4. 两大 Python 生态（移植最该分清的）

1. **`product/Script/`** —— 历史**采集脚本库**，由 `CollectItem/*.xml` 注册表驱动，单脚本做「发命令→正则取值→回写」。
2. **`services/EMTMessageAnalyseClientService/`** —— 独立且更大的**配置审计规则引擎**：
   - `app_data/script/<功能>/rule_*.py`：审计规则，导出 `check()`（BGP/OSPF/QOS/MPLS/L2/L3VPN/CLOCK/...）。
   - `collectscript/huawei/<族>/*.py`：采集器，导出 `collect()`，写入结构化 `model` 树。
   - `app_data/pdk/`：公共框架（CmdEchoParser / ParserTool / NeAssistant）。
   - 这是**真正可搬的"规则知识库"**，比 `product/Script` 更系统、按协议功能分类。

---

## 5. 端到端数据流

```
设备(SSH/Telnet/Netconf/SNMP)
   │  NMOTBusinessService 构造连接参数 (BuildSSHData...)
   ▼
协议组件（位于桌面客户端二进制，非本包 jar）建立会话
   │  下发 CLI 命令（bigCommandConfig.xml 标记大命令流式处理）
   ▼
回显文本
   ├─► product/Script/*.py  (CollectItem 注册表派发) ──┐
   ├─► EMT collectscript/*.py (collect()→model 树)    ├─► 结构化结果
   └─► EMT app_data/script/*.py (check()→CheckResult)─┘
   │
   ▼
IPDeviceCheckService：命令回显 → 正则解析(xmlconfig) → 字段算子 ETL → 按厂商 Saver 落地 DB
   │  + 链路还原(linkrestoration) + 弱光/光模块比对(lowlight) + 交叉光纤(crossconnectedfiber)
   ▼
DB (dbScript 模型)  →  报表导出(Excel/CSV) / 前端展示
```

---

## 6. 移植价值排名（按性价比）

| 优先级 | 模块 | 落地位置 | 说明 |
|---|---|---|---|
| ★★★ | EMT 配置审计规则库 | `02` | 8312 条按协议分类的 `check()` 规则，是最大可复用知识资产 |
| ★★★ | 声明式解析规则引擎 | `02`+`03` | CmdEchoParser 规则树 + IPDeviceCheck 的 xmlconfig 正则 |
| ★★ | NMOT 采集编排/协议层 | `04` | 连接参数模型、脚本桥(38887)、任务调度、Agent 推送 |
| ★★ | 设备形态/版本映射 | `05` | devicemapping/productItem.xml、versionmapping、getversion |
| ★★ | 数据库模型 | `06` | 设备/任务/采集项/结果/硬件(CEAS) 全量 schema |
| ★ | 配置审计/入参校验 | `05` | validate/*.xml、riskCmd、安全白名单 |
| ★ | 应用模块边界 | `07` | 5 个 APP 的功能划分，对应交付工具的功能菜单 |

---

## 7. 文档索引

| 文档 | 覆盖目录 | 重点 |
|---|---|---|
| `01-product-采集脚本体系.md` | `product/` | 注册表 + 脚本库 + 模板 + 资源 |
| `02-EMTMessageAnalyse-配置审计规则引擎.md` | `services/EMTMessageAnalyseClientService/` | 审计规则库 + 采集器 + 框架 |
| `03-IPDeviceCheckService-回显解析与弱光光纤.md` | `services/IPDeviceCheckService/` | 正则解析引擎 + 字段算子 + 弱光光纤 |
| `04-NMOT-采集编排与协议层.md` | `services/NMOT*`、`NetCareInsideService` | 连接/脚本桥/调度/Agent |
| `05-config-配置体系.md` | `config/`、`bin/`、`launcher/` | 设备映射/版本映射/命令/校验/风险 |
| `06-dbScript-数据模型.md` | `dbScript/` | 全量表结构 |
| `07-apps-应用模块.md` | `apps/` | 5 个 APP 功能边界 |
| `08-NetWeaverGo-移植能力映射与路线图.md` | 汇总 | 资产→NetWeaverGo 映射 + 优先级 |
