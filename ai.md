1. 如果需要执行编译，使用根目录的build.bat编译脚本；
2. 当前环境是windows，积极使用powershell命令进行文件搜索等你需要的功能；
3. 输出的文档放在根目录的docs目录下；
4. 该项目为新建项目，在输出修复方案时，无需考虑兼容历史代码，按照新建项目进行修复；
5. 输出方案时，必须考虑适配整体项目架构，严禁只考虑一个函数、一个模块，严禁只针对一小块代码进行打补丁操作，必须考虑全局。
