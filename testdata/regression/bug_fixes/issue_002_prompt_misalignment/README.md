# Issue 002: 提示符错位问题

## 问题描述

命令输出后的提示符可能被错误地拼接到正文末尾，导致解析错误。

## 输入特征

- 命令输出后紧跟设备提示符
- 提示符格式为 `<设备名>`

## 期望输出（待实现）

- 提示符不进入规范化输出
- 正文内容完整保留

## 当前状态

**状态**: 待实现

当前 Replayer 尚未实现提示符识别和移除功能。提示符会作为普通行保留在输出中。

## 修复方案

使用 `terminal.Replayer` 正确识别行边界，提示符作为独立行处理。
需要在 SessionMachine 层面实现提示符识别逻辑。
